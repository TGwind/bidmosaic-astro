# Plan

1. Add server-side storage/index module (read/write, rebuild indexes)
2. Add admin UI + API routes (upload post, upload assets, publish, rebuild)
3. Switch blog pages to SSR loaders (filesystem-backed)
4. Document env vars and a seed-import script

