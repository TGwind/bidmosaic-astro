# Workflow

- Prefer small, incremental changes with a runnable dev server.
- Any new dependency must be recorded in `conductor/tech-stack.md`.
- Blog content changes flow through Keystatic and Git, then CI/CD build & deploy.
